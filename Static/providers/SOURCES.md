# Fontes dos ícones de provedor

Um ícone por linha. Prioridade: [Simple Icons](https://simpleicons.org/) (MIT), depois Wikimedia Commons. Ícones de protocolo sem marca usam arte RDrive.

- `azureblob.svg` — existente (normalização) — —
- `b2.svg` — existente (normalização) — —
- `box.svg` — existente (normalização) — —
- `drive.svg` — existente (normalização) — —
- `dropbox.svg` — existente (normalização) — —
- `ftp.svg` — existente (normalização) — —
- `gcs.svg` — existente (normalização) — —
- `generic.svg` — RDrive / existente — Original
- `hdfs.svg` — existente (normalização) — —
- `local.svg` — RDrive / existente — Ícone pasta local (original)
- `mega.svg` — existente (normalização) — —
- `onedrive.svg` — existente (normalização) — —
- `pcloud.svg` — RDrive / existente — Arte simplificada — sem slug Simple Icons
- `s3.svg` — existente (normalização) — —
- `sftp.svg` — RDrive / existente — Ícone protocolo (original)
- `sharepoint.svg` — existente (normalização) — —
- `smb.svg` — RDrive / existente — Ícone protocolo (original)
- `terabox.svg` — RDrive (geométrico, cores marca TeraBox) — Original — não é logótipo oficial; substituir se obtiver asset licenciado
- `webdav.svg` — RDrive / existente — Ícone protocolo (original)
