"""Ícones SVG minimalistas por backend rclone (não-oficiais, cores de marca aproximadas).

Estrutura por tipo de backend (não espelha abas Pessoal/Empresarial da UI):

- ``cloud/`` — SaaS (Drive, Dropbox, OneDrive, Box, …)
- ``storage/`` — object storage (S3, B2, GCS, Azure Blob, …)
- ``protocol/`` — WebDAV, SFTP, FTP, SMB, HDFS, …
- ``local/`` — disco local / alias / mount
- ``_fallback/`` — ícone genérico
"""